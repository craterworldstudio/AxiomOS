[package]
name = "axiom_kernel"
version = "0.1.0"
edition = "2021"
authors = ["Luna", "Gemini"]
description = "Axiom OS Bare-Metal Substrate"

[dependencies]
# Currently empty. We rely on nothing but core.